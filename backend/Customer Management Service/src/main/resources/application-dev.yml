spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cms_db
    username: cms_user
    password: cms_pass

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace   # show SQL bind params (Hibernate 6)

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://131.163.97.60:8761/eureka/}
  instance:
    prefer-ip-address: true
    ip-address: 18.138.251.174